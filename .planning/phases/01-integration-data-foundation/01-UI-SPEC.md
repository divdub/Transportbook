---
phase: "1"
slug: "integration-data-foundation"
status: draft
shadcn_initialized: false
preset: none
created: "2026-08-30"
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> Phase 1 scope: **AUTH + DATA FOUNDATION** — splash, welcome, role-aware login (owner OTP / driver-pump-employee OTP+password), business-setup self-serve tenant creation, logout, and the shared adapter's loading / empty / error / offline-aware / session-expiry states. No production module falls back to mock data (DATA-01).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (manual theme tokens — **not** shadcn) |
| Preset | not applicable — React Native, no `components.json` |
| Component library | none (app-owned `src/components/common` + `src/components/ui`) |
| Icon library | react-native-vector-icons / MaterialCommunityIcons |
| Font | system default (`typography.fontFamily` = undefined) |
| Styling | `StyleSheet.create` + tokens from `src/theme` (`colors`, `spacing`, `radius`, `shadows`, `typography`) via barrel `src/theme/index.js` |

**Source of truth:** This contract documents the EXISTING design system found in `src/theme/*.js`, `src/components/common/*`, `src/components/ui/*`, and the existing auth screens. It does **not** invent a parallel design system. All values below are pre-populated from the codebase (provenance: read from source, 2026-08-30) unless marked `default`.

---

## Component Inventory

> The project's design system is the app's own shared component set — not a third-party package.
> Enumerated from the codebase with `ls`, not from recall.

Enumerated by `ls src/components/common/*.js src/components/ui/*.js 2>/dev/null | wc -l` — 7 components — app-internal (React Native 0.87.0) — 2026-08-30.

This is a **non-exhaustive** list of known-good components, never a closed allowlist — the executor may use anything the app exports, and checking outside the table is the expected path.

| Component | Import path | Notes |
|-----------|-------------|-------|
| AppText | `src/components/common/AppText` | Text with variants `title/heading/body/label/caption`; `color` prop maps to `colors` tokens |
| AppButton | `src/components/common/AppButton` | Primary/secondary variants; `disabled`, `pressed` states; min-height 48 |
| AppCard | `src/components/common/AppCard` | Surface card with hairline border + `shadows.card` |
| AppScreen | `src/components/common/AppScreen` | Safe-area + scroll container on `colors.background`, padding `spacing.xl` |
| AppHeader | `src/components/common/AppHeader` | Title + optional muted subtitle |
| EmptyState | `src/components/common/EmptyState` | Centered title + message + optional action button; **use for empty states** |
| StatusBadge | `src/components/ui/StatusBadge` | Pill badge; tones `success/warning/danger`; **missing role tone — see Role/Asset section** |

**Phase 1 additions (gaps this contract requires):**
| Component | Import path | Notes |
|-----------|-------------|-------|
| `RoleBadge` (or `StatusBadge` tone `role`) | `src/components/ui/` | New — renders 4 role chips (owner/driver/pump/employee). See Role-aware section |
| `AppLoadingState` (or reuse RN `ActivityIndicator` inline) | `src/components/common/` | New — standard full/section loading indicator |
| `OfflineBanner` (or `StatusBadge` tone `offline`) | `src/components/common/` | New — persistent offline indicator at top of data screens |

---

## Spacing Scale

Declared from the existing `src/theme/spacing.js` tokens (provenance: codebase). The primary
8-point spine is **4, 8, 16, 24, 32, 48, 64** — the binding set for any spacing newly introduced
in Phase 1. Three legacy tokens (`md=12`, `xl=20`, `4xl=40`) break strict 8-point but are
pre-existing and wired throughout — **they are frozen as legacy, do not extend the pattern with new
non-multiple-of-4 values, and do not treat 40 as a new precedent.**

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, label↔input gap, badge padding-y |
| sm | 8px | Compact element spacing, field-group gaps |
| md | 12px (legacy) | Default field gaps; **legacy, frozen** |
| lg | 16px | Section padding, default card gaps, horizontal screen padding |
| xl | 20px (legacy) | OTP row / phone-row gaps; **legacy, frozen** |
| 2xl | 24px | Layout gaps — header, card↔form, tab bar |
| 3xl | 32px | Major section breaks, splash main gap |
| 4xl | 40px (legacy) | Page-level spacing, empty-state padding-y; **legacy, frozen** |
| 5xl | 48px | Page-level spacing (splash top) |

Exceptions: **none for Phase 1 beyond the noted legacy 12/20/40px tokens.** All NEW spacing
introduced in Phase 1 draws only from {4, 8, 16, 24, 32, 48, 64}. Touch targets: minimum
interactive height is 48px (AppButton base) and 44–56px for OTP input boxes (width 48 / height 56
in `OtpScreen.js`, 44×48 in `BusinessSetupScreen.js`). Back/icon buttons: 38px box (`AuthScreen.js`)
— keep ≥ 38px with adequate hit padding.

---

## Typography

Declared from the existing `src/theme/typography.js` sizes/weights and the `AppText` variants
(provenance: codebase). The contract binds typography through the **`AppText` variant roles** below
— the executor writes against variants, not raw sizes/weights.

**Binding set (what Phase 1 may draw from):** **4 sizes** — `28` (title), `22` (heading),
`16` (body), `14` (label) — and **2 weights** — `400` (regular) + `600` (semibold) as the emphasis
axis. `caption` (12px, 500) is **folded into `label` (14px)** for new Phase 1 screens.

| Role (AppText variant) | Size | Weight | Line Height | Usage in Phase 1 |
|------|------|--------|-------------|------------------|
| title | 28 | **600** semibold* | 34 | Splash brand, screen titles (AppHeader), Business-setup heading |
| heading | 22 | **600** semibold | 28 | Section headers, empty-state titles, card titles, role-selection labels |
| body | 16 | **400** regular | 24 | Body copy, subtitles, hero copy, form help text |
| label | 14 | **400** regular\* | 20 | Input labels, button labels, field values, table/row labels; **also absorbs the former `caption` role** (helper text, OTP hints, timestamps, badge labels, footer brand text) |

\* ⚠️ **Legacy tie-outs (non-binding, documented for reference only):** the existing `AppText`
theme maps `title`→700, `label`→500, and exposes `caption` (12px, 500). These theme values are the
app's full type system and are **PRESERVED in `src/theme/typography.js`**; however, they are
**non-binding legacy tokens.** Newly-authored Phase 1 screens must draw sizes from the binding set
above and use only the **600/400** weight axis. Where the theme's `title`/`label` defaults resolve
to 700/500, explicitly pass `weight={600}` (title/label-emphasis) or `weight={400}` (label/body) so
Phase 1 output stays within the ≤2-weight contract. The former `caption` role is deprecated in
favor of `label` for all new Phase 1 copy.

**Line-height convention:** body 1.5 (24/16), title 1.21 (34/28), heading 1.27 (28/22). These are
the pre-existing AppText values — do not override.

**Weights bound for Phase 1:** **400 (regular)** and **600 (semibold)**. The 600 emphasis axis is
reserved for titles, headings, and emphasized labels; 400 is the default body/label weight. The
full app theme still carries 500/700 legacy mappings, but Phase 1 screens must not introduce new
dependencies on them.

---

## Color

Declared from the existing `src/theme/colors.js` tokens (provenance: codebase). Mapped to the
60/30/10 contract using the app's actual palette. Accent is the **navy primary** (`#08205B`), not a
separate accent token.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `background` `#F6F7F9` | Screen backgrounds (`AppScreen`), AppNavigator/AuthNavigator content + top-level nav header |
| Secondary (30%) | `surface` `#FFFFFF` | Cards, white floating login card (`AuthScreen`), input fields, tab bar, nav surfaces |
| Secondary (tint) | `surfaceMuted` `#F2F4F7`, `surfaceSubtle` `#F8F9FA` | OTP-box unfilled, country-code chip, subtle fills |
| Accent (10%) | `primary` `#08205B` (deep navy) + `primary2` `#06163E` | **Reserved-for:** primary buttons, brand mark / logo badge, OTP filled + focus borders, tab active tint, links (`.switchLink`, "Change number", "Resend OTP"), progress bar, splash progress track |
| Accent soft | `primarySoft` `#EEF2FF` | Secondary button fill, focused tab glyph background |
| Text | `text` `#121826`; `textMuted` `#667085` | Body text; secondary/subtitle text, placeholders |
| Border | `border` `#E3E7EE` | Input borders, card hairlines, tab bar top border |
| Destructive | `danger` `#B42318` (+ `dangerSoft` `#FEE4E2`) | **Destructive actions only** — field error text, error banner, log-out confirm accent |
| Success | `success` `#15803D` (+ `successSoft` `#DCFCE7`) | Mobile "Verified" badge (Business setup) |
| Warning | `warning` `#B45309` (+ `warningSoft` `#FEF3C7`) | Session-expiry (near) / offline warnings if tonal |

**Accent reserved for:** primary buttons, brand mark, links, tab active tint, OTP focus/filled,
progress indicators, and the role-badge owner emphasis. **Never** use accent for body text, card
tints, or destructive surfaces.

**Destructive reserved for:** field validation errors, network/error banners, and the logout
confirmation trigger. No other destructive actions exist in Phase 1.

---

## Copywriting Contract

Grounded in the exact screens Phase 1 touches. **Primary navigation verb is "Continue"; the phase's
auth CTAs are "Send OTP", "Verify & Continue", and "Complete Signup."** All copy is US-English,
`start case` on buttons, sentence case elsewhere.

| Element | Copy |
|---------|------|
| Welcome CTA | **Continue with Mobile** → opens role-aware login |
| Login CTA (mobile step) | **Send OTP** (idle) / **Sending...** (busy) — disabled until 10-digit mobile entered |
| Login CTA (OTP step) | **Verify & Continue** (idle) / **Verifying...** (busy) — disabled until 6 digits complete |
| Password step CTA (driver/pump/employee) | **Log in** / **Logging in...** |
| Owner login (OTP only) | After Verify & Continue → straight to app (no password step) |
| Resend OTP | **Resend OTP** (enabled) / **Resend code in 0:NN** (countdown, disabled) |
| Change number | **Change number** — returns to mobile entry, clears OTP |
| Business setup CTA | **Complete Signup** (disabled until mobile verified) |
| Business setup mobile verify | **Verify** / **Sending...** then **Verify OTP** / **Verifying...** |
| Verified badge | **Verified** (success tone, check-circle icon) |
| Empty state heading | **Nothing here yet** |
| Empty state body | **No data to show right now. Pull to refresh or try again in a moment.** (+ optional **Create / Retry** action when a CTA applies) |
| Error state (validation) | **[Specific field message]** e.g. "Enter a valid 10-digit mobile number", "OTP must be 6 digits" — shown under field, `danger` tone |
| Error state (network/offline) | **You're offline** / **Can't reach the server right now. Check your connection and try again.** |
| Error state (server) | **Something went wrong on our side. Please try again in a moment.** (+ **Retry** action) |
| Session expired | **Your session has expired.** / **For security, please log in again. Your unsaved work is safe.** |
| Logout confirmation | Trigger: **Log out**. Confirm dialog: **Log out?** / body: **Ending the session also ends it on the server. You'll need to sign in again.** Buttons: **Cancel** / **Log out** (danger). |
| Login error fallback | **Unable to log in. Please try again.** |

**Destructive actions in phase:** exactly **one** — **Log out** (server-side session end, AUTH-05).
Confirmation approach: in-app confirmation dialog (`Alert.alert`) with explicit Cancel / Log out; the
destroy action is the only `danger`-toned interactive element. No other destructive element exists.

---

## Role & Asset Considerations (Phase 1 additions)

These are gaps this contract adds — they do not exist in the current codebase:

1. **Role: 4 hard-coded roles** from the backend — `owner` (1), `driver` (2), `petrol-pump` (3),
   `employee/admin` (4). Phase 1 introduces **role-aware login**: the mobile/OTP step is shared;
   after the mobile is identified, the credential step branches (owner → OTP only; driver/pump/
   employee → OTP + password). The UI must surface which role is logging in.
   - **New `RoleBadge`** (or `StatusBadge` `role` tones): pill showing the current role on any
     post-login screen touched by Phase 1 (e.g. a session/user header not present today). Label
     mapping: Owner / Driver / Petrol Pump / Employee.
2. **Offline awareness (DATA-02):** an `OfflineBanner` at the top of data surfaces when the network
   is unreachable, using muted `warning`-adjacent tone. Distinct from the full-screen error state.
3. **Session-expiry (AUTH-07):** a non-destructive, dismissible banner/sheet on `authentication`
   (401) re-auth, preserving local state; re-authentication returns the user to where they were.

---

## UI Considerations

> State coverage per the ui-phase probe taxonomy (bytecode `ui-consideration-probe.cjs`,
> verified 2026-08-30). Copy for empty/error/offline lives in the Copywriting Contract above and is
> referenced (not restated) here. Loading is a Phase 1 hard requirement (DATA-02) — the shared
> adapter must expose loading consistently.

Probe result: 31 applicable considerations across 10 surfaces → 23 covered (explicit),
6 dismissed (reasoned), 2 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| loading | app boot / session restore | ✅ covered | `RootNavigator` shows centered `ActivityIndicator` tinted `colors.primary` while `isBootstrapping`. |
| loading | login mobile form, OTP form, password form, business-setup submit, logout round-trip | ✅ covered | CTA label swaps ("Sending... / Verifying... / Logging in... / Logging out...") while `isSubmitting`; AppButton `disabled` + 0.48 opacity; logout confirm dialog disables its buttons during the server round-trip. |
| loading / error | welcome screen | ✅ dismissed | Static screen — no async load or submit on it; CTA navigation defers all loading/error states to the login step. |
| empty | forms (E3–E6) | ✅ dismissed | Forms are not data lists; the unfilled state is already the empty affordance (placeholders + disabled CTA). No absent-data render applies. |
| error | login / OTP / password / business-setup; adapter network/server; logout failure | ✅ covered | Field-level `danger` message under input; network/server full-surface copy + **Retry** per Copywriting Contract; failed logout keeps the dialog open with error copy and a retry affordance. |
| partial | OTP gating (login + business setup) | ✅ covered | "Send OTP" disabled until 10 digits; "Verify & Continue" disabled until 6 digits; "Complete Signup" disabled until `mobileVerified`; OTP boxes get focus/filled tints only as digits are entered. |
| zero-one-many | role selection / RoleBadge (4 roles) | ✅ covered | Constant-width role chip renders consistently at 1 vs 4 roles; "Petrol Pump" label wraps within the chip. |
| overflow | offline banner / session-expiry banner | 🧪 backstop | Subtitle uses the maxWidth wrap pattern; held-out visual check that banner text reflows on narrow screens. |
| long-text | form errors + help copy, dialog body, welcome hero | ✅ covered | Wrap full-width (forms/dialog) or per width; `label`/`body` line-heights per typography table; no fixed-height text blocks. |
| populated | login + business setup happy path | ✅ covered | Single required field per step (mobile → OTP → [password]) keeps each screen short; standard 48px buttons. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| _none_ | _none_ | not applicable — React Native project; no shadcn, no third-party registries |

No third-party registries or external component blocks are used in Phase 1. All UI components are
app-owned (`src/components/common`, `src/components/ui`) or provided by the listed npm packages
(react-native-vector-icons, @gorhom/bottom-sheet, lottie-react-native, react-native-reanimated)
already installed in the project. Registry vetting gate: **not applicable.**

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS
- [ ] Dimension 7 Inventory Provenance: PASS

**Approval:** ✅ approved (all 7 dimensions PASS, verified 2026-08-30; UI Considerations probe written back)
