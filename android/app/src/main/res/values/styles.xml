<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Native launch window shown before React Native renders SplashScreen. -->
        <item name="android:windowBackground">#115E59</item>
        <item name="android:statusBarColor">#115E59</item>
        <item name="android:navigationBarColor">#115E59</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

</resources>
